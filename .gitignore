.claude
_bmad
.superpowers
.worktrees
app/test-results/
test-results/

working/
app/public/photos
app/public/music
app/public/videos

# MERIDIAN_DATA media folders — never tracked, except for the public demo
# datasets whose media ships with the repo so CI can deploy the demos.
data/*/photos
data/*/music
data/*/videos
!data/ny-trip/**
!data/love-story/**

# Local env overrides (Vite convention).
.env.local
.env.*.local
