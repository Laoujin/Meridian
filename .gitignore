.claude
_bmad
.superpowers
.worktrees
app/test-results/
test-results/

working/
app/public/photos
app/public/music
app/public/videos

# MERIDIAN_DATA media folders — never tracked, except for the public demo
# datasets whose media ships with the repo so CI can deploy the demos.
data/*/photos
data/*/music
data/*/videos
!data/ny-trip/**
!data/love-story/**
data/our-story

# Local env overrides (Vite convention). .env holds MERIDIAN_DATA for the
# triage tool / `bun run dev`; it's per-checkout, never committed.
.env
.env.local
.env.*.local

# Triage tool's per-checkout config (active story + photo folder).
tools/triage/triage.config.json
